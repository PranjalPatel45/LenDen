<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ always creates a system splash before Flutter's first
         frame. Make it a plain app-colored bridge with no launcher/Flutter
         icon; Flutter then immediately renders SplashScreen. -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/app_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/transparent_splash_icon</item>
        <item name="android:windowBackground">@color/app_background</item>
    </style>

    <style name="NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>
</resources>
